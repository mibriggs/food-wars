export type Option = {
	value: string;
	imageSource: string;
	message: string;
};

export type RadioOption = {
	value: string;
	heading: string;
	subHeading: string;
};

export type MultiSelectOption = {
	id: string;
	value: string;
}
